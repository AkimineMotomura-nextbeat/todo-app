# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /api                                  controllers.HomeController.index

## Todo
# 一覧
GET     /api/todo                             controllers.TodoController.list
# 個別
GET     /api/todo/$id<[0-9]+>                 controllers.TodoController.get(id: Long)
# 新規作成のPOST先
POST    /api/todo                             controllers.TodoController.store
# 編集後のPOST先
PUT     /api/todo/$id<[0-9]+>                 controllers.TodoController.update(id: Long)
# DELETE
DELETE  /api/todo/$id<[0-9]+>                 controllers.TodoController.delete(id: Long)

## Category
# 一覧
GET     /api/todo/category                    controllers.CategoryController.list
# 個別
GET     /api/todo/category/$id<[0-9]+>        controllers.CategoryController.get(id: Long)
#色一覧
GET     /api/todo/category/color              controllers.CategoryController.colorList
# 新規作成のPOST先
+nocsrf
POST    /api/todo/category                    controllers.CategoryController.store
# 編集後のPOST先
PUT     /api/todo/category/$id<[0-9]+>        controllers.CategoryController.update(id: Long)
# DELETE
+nocsrf
DELETE  /api/todo/category/$id<[0-9]+>        controllers.CategoryController.delete(id: Long)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                         controllers.Assets.versioned(path="/public", file: Asset)
